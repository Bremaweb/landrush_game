This mod just overrides blocks defined in default.

Currently it replaces:
  * default:ice

Dependencies are set to default, connected_textures just to load after these mods which also are modifying ice block.

Author: pitriss
Licence: WTFPL